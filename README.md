# Lessing : A quick way to start using LESS

This project is a [GulpJS](http://gulpjs.com/) sample.

To use it write some `.less` files in the `src` folder and run `gulp`.

To watch file changes, run `gulp watch`.